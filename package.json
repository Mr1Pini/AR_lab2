{
  "name": "ar_lab2",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && gh-pages -d dist"
  },
  "devDependencies": {
    "@vitejs/plugin-basic-ssl": "^2.2.0",
    "gh-pages": "^6.3.0",
    "vite": "^8.0.0"
  },
  "dependencies": {
    "three": "^0.183.2"
  }
}
